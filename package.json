{
  "name": "store-location-finder",
  "version": "2.0.0",
  "description": "Store Location Finder App for Delivery Personnel with Vercel Backend",
  "scripts": {
    "dev": "concurrently \"vercel dev\" \"serve -s . -l 3000\"",
    "build": "prisma generate",
    "start": "vercel dev",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "prisma": "^5.7.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.1",
    "vercel": "^32.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
